/**
 * TODO追加用のボタンを表示するコンポーネントです。
 * @returns {JSX.Element} TODO追加ボタンの見た目
 */
export function AddButton() {
  return (
    <button className="mt-6 text-lg font-semibold text-blue-500">
      ＋ 追加
    </button>
  );
}
